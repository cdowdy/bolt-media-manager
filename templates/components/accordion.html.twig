{% macro title(title) %}
    <h4 class="panel-title">
        <button class="panel-title-button" aria-expanded="false"
                type="button">
            {{ title }}
            <svg aria-hidden="true" focusable="false" viewBox="0 0 10 10">
                <rect class="vert" height="8" width="2" y="1" x="4"/>
                <rect height="2" width="8" y="4" x="1"/>
            </svg>
        </button>
    </h4>
{% endmacro title %}
